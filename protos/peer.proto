syntax = "proto3";

option go_package = "/peer";

message File {
    string name = 1;
    int32 size = 2;
}

message FileData {
    File file = 1;
    bytes data = 2;
}

message Empty {}

service Peer {
    rpc SendFile(FileData) returns (Empty) {}
}